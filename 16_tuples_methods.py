# Creating a tuple using ()
t = (1, 2, 3, 4, 5, 1, 2, 1)

print(t.count(1))        # Counts the given number
# print(t.index(3))          # shows the index number / given number is on which index number